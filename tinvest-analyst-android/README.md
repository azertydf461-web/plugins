# T-Invest Analyst — приложение для анализа рынка и консультативных выводов

Отдельное Android-приложение (собственный `applicationId` — `com.tinvestanalyst`),
ставится рядом с торговым ботом и работает независимо от него: общего кода,
общих настроек и общего токена у них нет.

## Ключевое отличие от бота

Приложение **физически не может совершить сделку**: в его API-клиенте нет ни
одного метода выставления заявок, только чтение рыночных данных. Поэтому для
него достаточно токена **только на чтение** — даже если вставить токен с
полным доступом, торговать приложение не станет.

## Что делает

- **Каталог доступных активов** — приложение само загружает с сервера брокера
  списки акций, облигаций, фондов, валют и фьючерсов и показывает только те,
  что доступны к торгам через API (`INSTRUMENT_STATUS_BASE` плюс проверка
  `apiTradeAvailableFlag` и `buyAvailableFlag`). Бумаги выбираются нажатием;
  угадывать тикеры и FIGI не нужно. Для бумаг «только для квалифицированных
  инвесторов» стоит отдельная пометка.
- **Список наблюдения** — плюс поиск по тикеру или названию, если нужна
  конкретная бумага.
- **Котировки в реальном времени** — пока экран открыт, цены обновляются раз
  в 15 секунд одним пакетным запросом на весь список (это бережёт лимиты API).
- **Свечной график** с наложенными SMA(9) и SMA(21) и гистограммой объёма,
  нарисованный на Canvas.
- **Сводная рекомендация** по шкале «активно покупать — покупать — держать —
  продавать — активно продавать» с процентом уверенности.
- **Разбор рекомендации по факторам** — главное: виден не только вывод, но и
  как он получен.

## Как считается рекомендация

Шесть независимых факторов, каждый со своим весом, дают оценку от `-вес` до
`+вес`; сумма нормируется и переводится в вердикт:

| Фактор | Вес | Что измеряет |
|---|---|---|
| Тренд SMA(9/21) | ±2 | направление тренда и момент пересечения средних |
| Долгосрочный тренд SMA(50) | ±1 | положение цены относительно длинной средней |
| RSI(14) | ±2 | перекупленность/перепроданность |
| MACD(12/26/9) | ±2 | смену импульса и её силу |
| Полосы Боллинджера(20, 2σ) | ±1 | статистическую растянутость движения |
| Объём | ±1 | подтверждён ли ход деньгами |

### Новостной фон

Брокерский API новостей не отдаёт, поэтому приложение читает открытые
RSS-ленты Интерфакса, РБК, Прайма и Финама, отбирает публикации про выбранную
бумагу (по тикеру, названию и списку синонимов вроде «Сбер» → SBER) и
оценивает тональность по словарю финансовых терминов. Свежесть учитывается:
вес публикации падает вдвое за сутки, старше четырёх суток — не учитывается.

Это оценка по словам, а не понимание смысла текста, поэтому заголовки с их
оценками показываются в карточке бумаги целиком — вывод можно перепроверить
глазами. Блок отключается в настройках одним переключателем.

Плюс ATR(14) — волатильность и ориентиры стопа (1.5 ATR) и цели (2.5 ATR),
и разбор стакана заявок (перевес покупателей или продавцов).

Каждый фактор показывает и свои числа, и словесную трактовку — вывод всегда
можно перепроверить руками.

## Проверка приложения

Отдельная вкладка «Проверка» отвечает на два вопроса, без которых любой
анализ остаётся красивым графиком.

**Журнал советов.** Каждый вердикт, кроме «держать», записывается вместе с
ценой, стопом и целью. Дальше по котировкам видно, дошёл ли он до цели, выбило
ли его стопом или истёк срок. Показывается доля сбывшихся советов и средний
результат. Судьба отслеживается по опросу цены, а не поминутно, поэтому
статистика — оценка, а не точный учёт сделок.

**Прогон на истории.** Та же логика, что стоит за вердиктом, прогоняется по
историческим свечам (дневные за 1–3 года или часовые за 4 месяца). Допущения
намеренно не в пользу стратегии: вход по открытию следующей свечи, комиссия и
спред на обеих сторонах, а при касании стопа и цели в одной свече
засчитывается стоп. Главное число отчёта — матожидание на сделку; рядом всегда
показывается результат простой покупки и удержания, потому что стратегия,
проигрывающая «купить и забыть», не нужна.

Проверяется только технический блок: отчётность, дивиденды и новости в прошлое
подставить нельзя — сегодняшние цифры тогда ещё не были известны, и их
подстановка дала бы фальшиво хороший результат.

## Ограничения, которые важно понимать

- Тональность новостей считается **по словарю, а не нейросетью**: сарказм,
  контекст и сложные формулировки она не понимает, поэтому вес новостного
  блока ниже, чем у цены и отчётности, а заголовки всегда показываются.
- Сопоставление новости и бумаги идёт по названию и тикеру: редкие эмитенты
  без узнаваемого имени в новостях могут не находиться вовсе.
- Это **не индивидуальная инвестиционная рекомендация**: приложение не знает
  ни целей, ни риск-профиля инвестора и никем не лицензировано как советник.
- Индикаторы запаздывают по своей природе: на резких новостных движениях они
  дают сигнал уже после факта.
- Данные берутся из REST API брокера с задержкой в единицы секунд — для
  внутридневной спекуляции на коротких таймфреймах этого может не хватать.

## Сборка

APK собирается в GitHub Actions workflow `build-tinvest-analyst-apk`
(артефакт `tinvest-analyst-debug-apk`) или локально в Android Studio:
JDK 17, compileSdk 34.
