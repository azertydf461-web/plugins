<?xml version="1.0" encoding="utf-8"?>
<!--
  Серверы Т-Банка используют сертификаты российского УЦ Минцифры, которого нет
  в хранилище Android — отсюда "Trust anchor for certification path not found".
  Пользователь ставит этот корневой сертификат на телефон сам, а приложение
  доверяет пользовательским сертификатам ТОЛЬКО для доменов брокера: для всего
  остального интернета остаётся обычная системная проверка.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">tbank.ru</domain>
        <domain includeSubdomains="true">tinkoff.ru</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
    <!--
      Новостные ленты: часть российских изданий тоже отдаёт сертификаты
      Минцифры. Доверие к пользовательским корневым сертификатам расширено
      ровно на эти домены и не затрагивает остальной интернет.
    -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">interfax.ru</domain>
        <domain includeSubdomains="true">rbc.ru</domain>
        <domain includeSubdomains="true">1prime.ru</domain>
        <domain includeSubdomains="true">finam.ru</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
